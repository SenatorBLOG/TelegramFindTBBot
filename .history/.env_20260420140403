# ────────────────────────────────────────────────
# Find the Travel Buddy — environment config
# Copy this file to `.env` and fill in real values
# ────────────────────────────────────────────────

# Telegram bot token from @BotFather
BOT_TOKEN=8761529824:AAETlENK9-SKrIUTTGDiquwV_l32-gipXF0

# Mode switch: TEST or PROD
# TEST  -> bot uses TEST_GROUP_ID
# PROD  -> bot uses PROD_GROUP_ID
MODE=TEST

# Forum-enabled supergroup chat IDs (negative numbers like -1001234567890)
# Use /chatid inside the group to obtain them.
TEST_GROUP_ID=-1001234567890
PROD_GROUP_ID=-1009876543210

# Your Telegram numeric user ID (the admin who can run /release).
# Obtain from e.g. @userinfobot.
ADMIN_USER_ID=1051988315

# Optional: override SQLite database path (default: data/bot.db)
# DB_PATH=data/bot.db
